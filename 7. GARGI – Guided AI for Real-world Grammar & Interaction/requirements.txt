# Core
numpy>=1.24
pandas>=2.0
requests>=2.31

# Speech I/O + audio
sounddevice>=0.4.6
soundfile>=0.12
librosa>=0.10

# Speech-to-text
openai-whisper>=20231117

# Language detection (if used in Stage 1)
langdetect>=1.0.9

# Grammar (client; server is external Java LanguageTool)
# (LanguageTool server runs separately via: java -jar languagetool-server.jar --port 8081)

# Topic relevance / NLP
sentence-transformers>=2.7
scikit-learn>=1.3
yake>=0.4.8

# Dashboard
streamlit>=1.33
matplotlib>=3.8

# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
